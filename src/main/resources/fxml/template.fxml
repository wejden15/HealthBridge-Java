<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<?import javafx.scene.layout.AnchorPane?>
<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="tn.esprit.controller.TemplateController"
            prefWidth="1200.0" prefHeight="700.0"
            stylesheets="@../css/style.css">

    <!-- Header -->
    <top>
        <VBox styleClass="header">
            <Label text="HealthBridge" styleClass="header-title"/>
        </VBox>
    </top>

    <!-- Sidebar -->
    <left>
        <VBox fx:id="sidebar"
              spacing="15"
              alignment="TOP_CENTER"
              styleClass="sidebar"
              prefWidth="200">

            <!-- Burger Menu Button -->
            <Button fx:id="toggleButton"
                    text="≡"
                    onAction="#toggleSidebar"
                    styleClass="burger-button"/>

            <!-- Logo (centered + margin) -->
            <ImageView fx:id="logoImage"
                       fitWidth="150"
                       preserveRatio="true"
                       style="-fx-margin-top: 10px;"/>

            <!-- Navigation Buttons -->
            <Button fx:id="homeButton" text="🏠 Accueil" onAction="#goToHome" styleClass="sidebar-button"/>
            <Button fx:id="calendarButton" text="📅 Calendrier" onAction="#goToCalendar" styleClass="sidebar-button"/>
            <Button fx:id="scanButton" text="📷 Scanner QR" onAction="#goToScanQR" styleClass="sidebar-button"/>
            <Button fx:id="patientButton" text="🤝 Participer Événement" onAction="#goToPatientEvents" styleClass="sidebar-button"/>
            <Button text="📄 Historique Donations"
                    onAction="#goToDonationHistory"
                    styleClass="sidebar-button"/>


        </VBox>


    </left>

    <!-- Main Content Area -->
    <center>
        <StackPane fx:id="contentArea" styleClass="content"/>
    </center>

</BorderPane>
